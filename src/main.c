#include "../inc/threadpool.h"

int main(int argc, char **argv) {
    // threadpool_t *threadpool = NULL;

    // threadpool_init(threadpool);
    // threadpool_add_task(threadpool, example_task, NULL);
    // threadpool_destroy(threadpool);
    return 0;
}